---
const { class: className } = Astro.props;
---

<card>
    <div
        class={`w-[75%] justify-center mx-auto mt-4 bg-gray-100 px-5 py-5 rounded-md ${className}`}
    >
        <slot />
    </div>
</card>
